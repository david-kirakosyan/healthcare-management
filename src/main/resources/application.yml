spring:
  flyway:
    locations: classpath:migrations
    url: jdbc:mysql://localhost:3306/healthcare_management?useUnicode=true
    user: root
    password: root

  datasource:
    url: dbc:mysql://localhost:3306/healthcare_management?useUnicode=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 100MB

